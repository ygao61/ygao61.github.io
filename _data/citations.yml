# Google Scholar citation counts, refreshed by .github/workflows/update-citations.yml
# using the scholar_userid in _data/socials.yml. Empty until that workflow first runs.
metadata:
  last_updated:
papers:
